# 德胧市场情报联邦 · 动态 Agent 部署模板（一键引入版）

> 下面这段是给兄弟酒店的开场白，可直接复制到微信/飞书转发。

---

【酒店市场情报 Agent · 一键引入】

这是一套可复制的酒店市场情报系统：把"10 个市场维度"
（休闲度假 / 企业协议 / 会员增购 / 餐饮宴会 / 长住公寓 / 数字渠道 /
会议会展 / 潜在客源 / 潜在广域 / TMC订单）"变成**按查询动态路由的 Agent**——
能对话触发、也能在飞书群里 @ 触发，还能主动监控推送市场机会。

你的酒店直接用，三步：

1. 打开模板库 👉 https://github.com/Chaoliuzhu/delonix-market-federation
   点右上角 **「Use this template」** 生成你自己的副本并 clone 到本地；
2. 装好 WorkBuddy 后，在仓库目录跑**一条命令**：
   `python3 init.py --hotel "你的酒店名" --geo "城市/区" --mode A`
   （脚本会自动生成你酒店专属的 10 个维度技能并部署进 WorkBuddy）
3. 按结尾提示把历史资料放进 `runtime/harvest/`（协议单位清单、飞书群历史等），
   情报会随着资料变准。

⚠️ 关键：系统产出精准度**取决于你喂的历史沉淀**。
补齐 P0 资料（协议单位 / 飞书群 / 酒店信息）前，能跑框架但不精准；
补完跑首轮建立去重基线后，情报才准。

想要**飞书群 @ 触发**或**主动监控推送**，用 `--mode B` 并配上各维度群 chat_id 即可。
任何问题拉群我帮调。

---

## 给技术同学（部署细节）

- 模板库已设为公开 Template，姐妹酒店 `Use this template` 一键派生自己的副本。
- `init.py` 一条命令完成：生成配置 → 生成你酒店 10 维技能 → 自动部署 12 个技能到 `~/.workbuddy/skills/` → 在 `runtime/harvest/` 生成带说明的历史资料占位 → 打印「引导补入历史数据」清单。
- 路径 A（纯 WorkBuddy 对话触发）零飞书依赖；路径 B（飞书群 @ 触发 + 主动监控）需飞书自建应用与 bot 进群。
- 仓库零真实数据（瑞湾实战样例已排除、token/群 ID 已清空），可安全公开。
- 详细见 `README.md` 与 `DEPLOY_GUIDE.md`。
